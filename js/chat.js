(() => {
 const form=document.querySelector('#composer'),input=document.querySelector('#messageInput'),conversation=document.querySelector('#conversation'),content=document.querySelector('.chat-content'),send=document.querySelector('#sendButton'),suggestions=document.querySelector('#suggestions');
 const reply='Вот с чего я бы начал:\n\n1. **Определим аудиторию.** Сформулируйте, для кого продукт решает самую болезненную задачу.\n2. **Соберём одно ясное обещание.** Оно должно объяснять ценность без профессионального жаргона.\n3. **Выберем первый проверяемый шаг.** Прототип, интервью или закрытый запуск — в зависимости от вашей гипотезы.\n\nРасскажите о продукте в двух предложениях — и я помогу превратить это в конкретный план.';
 const scroll=()=>conversation.scrollTop=conversation.scrollHeight; const resize=()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,160)+'px'}; const md=t=>t.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/\*\*(.*?)\*\*/g,'<strong>$1</strong>').replace(/\n/g,'<br>');
 async function sendMessage(message){/* Replace this function body with your API client when the backend is ready. */await new Promise(r=>setTimeout(r,420));return reply}
 async function submit(text){if(!text.trim())return;suggestions?.remove();let row=document.createElement('div');row.className='message-row user-row';row.innerHTML='<div class="bubble"></div>';row.querySelector('.bubble').textContent=text.trim();content.append(row);input.value='';resize();send.disabled=true;row=document.createElement('div');row.className='message-row assistant-row';row.innerHTML='<div class="assistant-avatar">✦</div><div class="bubble"><div class="typing-indicator"><i></i><i></i><i></i></div></div>';content.append(row);scroll();const answer=await sendMessage(text),bubble=row.querySelector('.bubble');let n=0;bubble.innerHTML='';const timer=setInterval(()=>{n+=3;bubble.innerHTML=md(answer.slice(0,n));scroll();if(n>=answer.length){clearInterval(timer);send.disabled=false;input.focus()}},12)}
 form.addEventListener('submit',e=>{e.preventDefault();submit(input.value)});input.addEventListener('input',resize);input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});document.querySelectorAll('.suggestion-grid button').forEach(b=>b.addEventListener('click',()=>submit(b.innerText)));
})();
